# Báo cáo Kiến trúc RAG Pipeline

**Họ Tên:** Hà Kế Trung Đức - MSSV: 2A202600594
**Ngày chạy:** 2026-06-08  

---

Dựa trên các file mã nguồn Python được phân tích tại thư mục `src`, dưới đây là báo cáo tổng hợp về toàn bộ quy trình (pipeline) của hệ thống Retrieval-Augmented Generation (RAG) được xây dựng cho dự án:

## 1. Data Ingestion & Preprocessing (Xử lý và Thu thập Dữ liệu)
* **Task 1: Thu thập tài liệu pháp luật (`task1_collect_legal_docs.py`)**
  * Thu thập các văn bản pháp luật chính thống (Bộ luật, Nghị định) liên quan đến ma túy (định dạng PDF/DOCX) và lưu trữ tại `data/landing/legal/`.
* **Task 2: Crawl báo chí (`task2_crawl_news.py`)**
  * Xây dựng script sử dụng `Playwright` và `BeautifulSoup` để cào (crawl) dữ liệu từ các trang báo (ví dụ VnExpress) về chủ đề nghệ sĩ và ma túy. Dữ liệu được lưu dạng JSON bao gồm metadata và nội dung tại `data/landing/news/`.
* **Task 3: Chuẩn hóa Markdown (`task3_convert_markdown.py`)**
  * Sử dụng thư viện `MarkItDown` (Microsoft) để chuyển đổi toàn bộ tài liệu thô (PDF, DOCX, JSON) sang định dạng Markdown chuẩn hóa và lưu tại `data/standardized/` để dễ dàng xử lý cho LLM.

## 2. Chunking & Indexing (Chia nhỏ và Lưu trữ)
* **Task 4: Phân mảnh và nhúng (`task4_chunking_indexing.py`)**
  * **Chunking:** Sử dụng `RecursiveCharacterTextSplitter` với `chunk_size=500` và `chunk_overlap=50`. Phương pháp này phù hợp để giữ lại trọn vẹn ngữ cảnh của đoạn văn.
  * **Embedding:** Sử dụng mô hình `text-embedding-3-small` của OpenAI, khắc phục hạn chế tương thích của `sentence-transformers` trên Python 3.14 đồng thời mang lại hiệu suất tốt cho tiếng Việt.
  * **Vector Store:** Indexing dữ liệu vào `ChromaDB` (Local Vector Store), sử dụng không gian khoảng cách Cosine.

## 3. Retrieval (Trích xuất thông tin)
* **Task 5: Tìm kiếm Ngữ nghĩa - Semantic Search (`task5_semantic_search.py`)**
  * Sử dụng Dense Retrieval thông qua Cosine Similarity để tìm kiếm các chunks có ý nghĩa tương đồng với câu truy vấn.
* **Task 6: Tìm kiếm Từ khóa - Lexical Search (`task6_lexical_search.py`)**
  * Sử dụng thuật toán `BM25` (`rank-bm25`) dựa trên tần suất từ khóa (TF-IDF) để bổ khuyết cho tìm kiếm ngữ nghĩa, đặc biệt quan trọng khi truy vấn các điều luật, mã số chính xác.

## 4. Advanced RAG (Nâng cao)
* **Task 7: Reranking (`task7_reranking.py`)**
  * Cung cấp hai cơ chế xếp hạng lại:
    1. **Cross-encoder:** Sử dụng Jina Reranker v2 qua API, đặc biệt mạnh mẽ đối với đa ngôn ngữ (tiếng Việt).
    2. **RRF (Reciprocal Rank Fusion):** Xếp hạng kết hợp (Semantic + Lexical) bằng công thức toán học để xử lý khi không sử dụng API ngoại.
* **Task 8: Vectorless RAG (`task8_pageindex_vectorless.py`)**
  * Tích hợp `PageIndex` để phân tích trực tiếp trên cây phân cấp của tài liệu (Hierarchical Tree Index) bằng LLM reasoning thay vì dùng Vector similarity. Chức năng này đóng vai trò là một cơ chế Fallback hiệu quả.
* **Task 9: Pipeline Tổng hợp (`task9_retrieval_pipeline.py`)**
  * Kết nối logic toàn bộ quá trình Retrieval: `Semantic` + `Lexical` -> `Merge (RRF)` -> `Rerank (Jina)` -> `Kiểm tra Threshold (Ngưỡng điểm)`. 
  * Nếu điểm của kết quả trả về nhỏ hơn ngưỡng (0.3), hệ thống sẽ tự động kích hoạt Fallback sang PageIndex (Task 8).

## 5. Generation (Sinh câu trả lời)
* **Task 10: Sinh phản hồi có Citation (`task10_generation.py`)**
  * **Mô hình:** Sử dụng `gpt-4o-mini` tối ưu chi phí và hiệu năng.
  * **Cấu hình sinh (Generation Config):** `top_k=5` (giới hạn context đủ evidence), `top_p=0.9` (duy trì tính đa dạng vừa đủ), `temperature=0.2` (tránh sáng tạo quá đà, bám sát factual law).
  * **Document Reordering:** Xử lý hội chứng `"Lost in the middle"` bằng cách sắp xếp lại vị trí các chunks (đưa các chunks quan trọng nhất lên đầu và cuối prompt).
  * **Prompting:** Yêu cầu LLM luôn đính kèm Citation/Nguồn (ví dụ: `[Luật Phòng chống ma tuý 2021]`) và tự động từ chối nếu context không chứa đủ bằng chứng.
